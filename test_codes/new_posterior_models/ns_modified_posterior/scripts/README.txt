
To run the programs, first run 
	"bash iter_codes.sh"
This needs to be modified in beforehand using the name of the datafile in each case. The code creates all the folders with the bash scripts needed for running the scripts

Afterwards, run 
	"bash exe_codes.sh" 
and the codes will be left in the queue

